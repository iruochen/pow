# -----------------------------------------------
# Go 语言编译与运行相关
# -----------------------------------------------

# 二进制文件和可执行文件 (你的 "pow" 文件)
# 这会忽略所有在项目根目录、或在当前目录下的 bin/ 目录中生成的同名文件
*.exe
*.dll
*.so
*.dylib
*.a
*.o
# 忽略你的具体可执行文件名称 (例如，如果你编译后的文件就是 pow)
pow

# 交叉编译生成的目录
bin/
pkg/

# -----------------------------------------------
# Go Module 依赖相关 (推荐)
# -----------------------------------------------

# 忽略 Go Module 下载的缓存，但保留 go.mod 和 go.sum
# 这个目录通常位于 GOMODCACHE, 但有时也会在项目目录中
/vendor/
